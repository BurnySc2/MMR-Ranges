[project]
name = "src"
version = "0.1.0"
requires-python = ">=3.8, <3.14"
dependencies = [
    "arrow>=1.3.0",
    "dpath>=2.2.0",
    "httpx>=0.28.0",
    "loguru>=0.7.2",
]

[dependency-groups]
dev = [
    "pyre-check>=0.9.23",
    "ruff>=0.8.2",
]
